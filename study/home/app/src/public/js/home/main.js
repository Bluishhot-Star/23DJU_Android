"use strict"
const toggleBtn = document.querySelector('.flex-container__toogleBtn');
const menu = document.querySelector('.flex-container__menu');
const icons = document.querySelector('.flex-container__link');


toggleBtn = addEventListener('click',function() {
    menu.classList.toggle('active');
    link.classList.toggle('active');
});
